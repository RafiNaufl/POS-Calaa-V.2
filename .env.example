# Database - PostgreSQL (Local Development)
# Sequelize will read `DATABASE_URL` first. Use a simple Postgres URL.
# Note: `schema=` parameters are Prisma-specific; remove them for Sequelize.
DATABASE_URL="postgresql://postgres:password@localhost:5432/pos_db"

# Optional: enable SSL (for hosted Postgres like Supabase, Vercel, Neon)
# Any of these flags set to true will enable SSL in Sequelize dialectOptions
# PGSSL=true
# PG_SSL=true
# POSTGRES_SSL=true

# Fallback connection fields (used if `DATABASE_URL` is not set)
# PGHOST="localhost"
# PGPORT="5432"
# PGUSER="postgres"
# PGPASSWORD="password"
# PGDATABASE="pos_db"

# Database - Vercel Deployment
# Uncomment these for Vercel Postgres deployment
# POSTGRES_PRISMA_URL="postgres://user:password@host:port/database"
# POSTGRES_URL_NON_POOLING="postgres://user:password@host:port/database"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here"

# Demo accounts (for development only)
# Admin: admin@pos.com / admin123
# Cashier: kasir@pos.com / kasir123

# Payment gateway configuration
# Midtrans configuration
MIDTRANS_MERCHANT_ID="your-merchant-id"
MIDTRANS_CLIENT_KEY="your-client-key"
MIDTRANS_SERVER_KEY="your-server-key"
MIDTRANS_IS_PRODUCTION=false
# Frontend → Backend base URL for API v1
NEXT_PUBLIC_BACKEND_URL="http://localhost:4000"