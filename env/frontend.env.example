# Frontend (Next.js) environment variables

# Public base URL to reach the backend Express API
NEXT_PUBLIC_BACKEND_URL="https://api.pos.example.com"

# NextAuth configuration (used by the Next.js app)
NEXTAUTH_URL="https://pos.example.com"
NEXTAUTH_SECRET="replace-with-strong-random-secret"

# Optional: Midtrans client key if frontend initiates Snap flows client-side
# Keep server key on the backend only
NEXT_PUBLIC_MIDTRANS_CLIENT_KEY=""

# Optional: Additional public flags
# NEXT_PUBLIC_APP_NAME="POS App"

